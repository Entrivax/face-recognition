# Build artifacts
recogn
recogn.exe
/dist
.gocache
.gopath
.gomodcache
web/node_modules

# Built web UI bundle (rebuilt via `make ui`); .gitkeep keeps the dir tracked
internal/web/dist/*
!internal/web/dist/.gitkeep

# Downloaded ONNX models (large; fetched via `make models`)
/models

# ONNX Runtime C library + headers (fetched via `make ort` / `make ort-win`)
third_party

# Generated face database (rebuilt via `recogn enroll`)
data

# Faces data
people

# Scratch
tmp
